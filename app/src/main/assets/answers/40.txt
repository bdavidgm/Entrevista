Concepto:
dp (density-independent pixels) y sp (scale-independent pixels) son unidades de medida que se adaptan a la densidad de la pantalla del dispositivo. 1 dp equivale a 1 píxel en una pantalla mdpi (160 dpi); en pantallas de mayor densidad el sistema escala automáticamente.

dp se usa para dimensiones (paddings, márgenes, tamaños), y sp se usa para texto, porque además respeta el tamaño de fuente configurado por el usuario en accesibilidad.

Ejemplo:
<TextView
    android:textSize="16sp"
    android:padding="8dp"
    android:layout_marginTop="12dp"/>

// Compose
Text("Hola", fontSize = 16.sp)
Spacer(Modifier.height(12.dp))

Consejo:
"dp para tamaños, sp para texto". Mencionar accesibilidad: si fuerzas textos en dp, los usuarios con texto grande no ven bien tu app.
