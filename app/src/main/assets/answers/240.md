## Concepto
Un WebSocket es una conexión TCP persistente entre cliente y servidor, iniciada con un handshake HTTP y luego bidireccional: ambos lados pueden enviar frames en cualquier momento sin abrir una petición nueva por mensaje. Sirve para chat en vivo, cotizaciones, presencia o juegos; evita polling constante de REST.

En Android suele implementarse con OkHttp (`newWebSocket`) o con clientes como Ktor. Hay que enlazar el ciclo de vida (pausar/cerrar al salir de primer plano si no usas FGS) y reconectar con backoff ante cortes de red.

Ejemplo:
val client = OkHttpClient()
val request = Request.Builder().url("wss://echo.websocket.events").build()

val socket = client.newWebSocket(request, object : WebSocketListener() {
    override fun onOpen(webSocket: WebSocket, response: Response) {
        webSocket.send("""{"type":"hello"}""")
    }
    override fun onMessage(webSocket: WebSocket, text: String) {
        // procesar JSON / texto
    }
    override fun onFailure(webSocket: WebSocket, t: Throwable, response: Response?) {
        // reintentar con delay exponencial
    }
})

// Al destruir el scope de la pantalla:
socket.close(1000, "bye")

ExplicaciónKotlin:
`val client = OkHttpClient()` declara cliente HTTP reutilizable; OkHttp es biblioteca Java/Kotlin interop ampliamente usada con Retrofit. `Request.Builder().url("wss://...").build()` encadena configuración; `wss` implica TLS como HTTPS. `client.newWebSocket(request, object : WebSocketListener() { ... })` pasa un objeto anónimo que sobrescribe callbacks Java: `onOpen`, `onMessage`, `onFailure`. En `onOpen`, `webSocket.send("""{"type":"hello"}""")` usa raw string triple comilla Kotlin para JSON sin escapar comillas. `onMessage` recibe `text: String` ya materializado; podrías parsear con `kotlinx.serialization` o Moshi. `onFailure` incluye `Throwable?` y `Response?` nullable para diagnosticar cortes de red. `socket.close(1000, "bye")` usa código de cierre WebSocket estándar; conviene llamarlo desde `onDestroy` o `viewModelScope.cancel()`. Los listeners se ejecutan en threads de OkHttp, no en Main: actualizar UI requiere `withContext(Dispatchers.Main)` o `runOnUiThread`. El patrón object anónimo es Kotlin clásico para adaptar APIs callback-based.

Consejo:
Usa ping/pong o heartbeats si el proxy corta conexiones idle. Valida TLS y certificados como en HTTPS. Si solo necesitas servidor→cliente con baja latencia, compara con SSE o FCM data antes de añadir complejidad de sockets.
