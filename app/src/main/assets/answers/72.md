## Concepto
WorkManager es la API recomendada para programar trabajo en background que debe completarse aunque la app se cierre o el dispositivo se reinicie. Soporta constraints (red, batería, carga, almacenamiento), reintentos con backoff y cadenas de tareas dependientes.

Sustituye a JobScheduler, AlarmManager y FirebaseJobDispatcher para trabajo diferible. Para tareas que deben ejecutarse en un instante exacto (alarmas, calendario), usa AlarmManager.

Ejemplo:
class SyncWorker(ctx: Context, params: WorkerParameters) : CoroutineWorker(ctx, params) {
    override suspend fun doWork(): Result {
        return try { repository.sync(); Result.success() }
        catch (e: Exception) { Result.retry() }
    }
}

val request = OneTimeWorkRequestBuilder<SyncWorker>()
    .setConstraints(Constraints.Builder().setRequiredNetworkType(NetworkType.CONNECTED).build())
    .build()
WorkManager.getInstance(context).enqueue(request)

ExplicaciónKotlin:
class SyncWorker(ctx: Context, params: WorkerParameters) : CoroutineWorker(ctx, params) hereda CoroutineWorker pasando contexto Android al padre. override suspend fun doWork(): Result { ... } redefine método suspend que devuelve Result de WorkManager. try { repository.sync(); Result.success() } catch (e: Exception) { Result.retry() } usa try/catch con e: Exception tipado; repository.sync() presumiblemente suspend; Result.success() y Result.retry() son factory estáticos. OneTimeWorkRequestBuilder<SyncWorker>() usa genérico reified en builder para tipo Worker. .setConstraints(Constraints.Builder().setRequiredNetworkType(NetworkType.CONNECTED).build()) encadena builder Java/Kotlin style. .build() crea OneTimeWorkRequest. WorkManager.getInstance(context).enqueue(request) obtiene singleton y encola; context debe ser Context de Android.

Consejo:
"Diferible y persistente; respeta Doze y battery savers". Usa OneTimeWork o PeriodicWork según el caso y enqueueUniqueWork para evitar duplicados.
