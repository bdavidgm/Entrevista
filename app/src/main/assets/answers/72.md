## Concepto
WorkManager es la API recomendada para programar trabajo en background que debe completarse aunque la app se cierre o el dispositivo se reinicie. Soporta constraints (red, batería, carga, almacenamiento), reintentos con backoff y cadenas de tareas dependientes.

Sustituye a [JobScheduler](pregunta:189), [AlarmManager](pregunta:175) y FirebaseJobDispatcher para trabajo diferible. Para tareas que deben ejecutarse en un instante exacto (alarmas, calendario), usa [AlarmManager](pregunta:175).

Ejemplo:
class SyncWorker(ctx: Context, params: WorkerParameters) : CoroutineWorker(ctx, params) {
    override suspend fun doWork(): Result {
        return try { repository.sync(); Result.success() }
        catch (e: Exception) { Result.retry() }
    }
}

val request = OneTimeWorkRequestBuilder<SyncWorker>()
    .setConstraints(Constraints.Builder().setRequiredNetworkType(NetworkType.CONNECTED).build())
    .build()
WorkManager.getInstance(context).enqueue(request)

ExplicaciónKotlin:
class SyncWorker(ctx: [Context](pregunta:127), params: WorkerParameters) : CoroutineWorker(ctx, params) hereda CoroutineWorker pasando contexto Android al padre. [override](pregunta:247) [suspend](pregunta:79) fun doWork(): Result { ... } redefine método [suspend](pregunta:79) que devuelve Result de WorkManager. try { [repository](pregunta:63).sync(); Result.success() } catch (e: Exception) { Result.retry() } usa try/catch con e: Exception tipado; [repository](pregunta:63).sync() presumiblemente [suspend](pregunta:79); Result.success() y Result.retry() son factory estáticos. OneTimeWorkRequestBuilder<SyncWorker>() usa genérico [reified](pregunta:23) en builder para tipo Worker. .setConstraints(Constraints.Builder().setRequiredNetworkType(NetworkType.CONNECTED).build()) encadena builder Java/Kotlin style. .build() crea OneTimeWorkRequest. WorkManager.getInstance([context](pregunta:127)).enqueue(request) obtiene singleton y encola; [context](pregunta:127) debe ser [Context](pregunta:127) de Android.

Consejo:
"Diferible y persistente; respeta Doze y battery savers". Usa OneTimeWork o PeriodicWork según el caso y enqueueUniqueWork para evitar duplicados.
