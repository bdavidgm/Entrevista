## Concepto
Una migración en [Room](pregunta:70) define cómo evolucionar el esquema de la base de datos de una versión a la siguiente sin perder datos. Cada vez que cambias entidades (nuevas tablas, columnas, índices) debes subir la versión y registrar una Migration que ejecute el SQL necesario.

Sin migración (o con fallbackToDestructiveMigration), [Room](pregunta:70) borra y recrea la BBDD: aceptable en debug, inaceptable en producción.

Ejemplo:
val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(db: SupportSQLiteDatabase) {
        db.execSQL("ALTER TABLE User ADD COLUMN avatarUrl TEXT")
    }
}

Room.databaseBuilder(context, AppDb::class.java, "app.db")
    .addMigrations(MIGRATION_1_2)
    .build()


ExplicaciónKotlin:
`val MIGRATION_1_2 = object : Migration(1, 2)` crea instancia anónima de objeto que extiende `Migration`, pasando versiones origen y destino al constructor superior. `override fun migrate(db: SupportSQLiteDatabase)` sobrescribe método abierto; el parámetro `db` expone API SQLite de soporte. `db.execSQL(...)` ejecuta DDL como string. `Room.databaseBuilder(context, AppDb::class.java, "app.db")` usa referencia Java `::class.java` de la [KClass](pregunta:256) para el tipo de la base de datos — puente Kotlin/Java habitual en [Room](pregunta:70). `.addMigrations(MIGRATION_1_2)` encadena configuración con expresión fluente y `.build()` materializa la instancia. El objeto anónimo mantiene una única migración nombrada como constante de nivel superior (`val` inmutable).

Consejo:
Activa exportSchema = true y commitea los JSON del esquema; permite migraciones automáticas en versiones recientes de [Room](pregunta:70) y revisión en CI.
