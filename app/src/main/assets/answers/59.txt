Concepto:
Scaffold es un Composable de Material que ofrece el esqueleto de una pantalla con slots predefinidos: topBar, bottomBar, floatingActionButton, snackbarHost y contenido principal. Gestiona automáticamente los insets y el espaciado para que el contenido no quede tapado por las barras.

Es el punto de partida estándar para pantallas con TopAppBar, FAB o snackbars.

Ejemplo:
@Composable
fun HomeScreen() {
    val snackbarHostState = remember { SnackbarHostState() }
    Scaffold(
        topBar = { TopAppBar(title = { Text("Inicio") }) },
        snackbarHost = { SnackbarHost(snackbarHostState) },
        floatingActionButton = { FloatingActionButton(onClick = { }) { } }
    ) { padding ->
        LazyColumn(contentPadding = padding) {
            items(users) { UserRow(it) }
        }
    }
}

Consejo:
"Aplica siempre el paddingValues del Scaffold al contenido", si no, queda tapado por la TopAppBar.
