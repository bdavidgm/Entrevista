Concepto:
App Startup es una librería de Jetpack que permite inicializar componentes al arrancar la app de forma ordenada y sin tener que crear múltiples ContentProvider de inicialización. Defines clases Initializer<T> que declaran sus dependencias y App Startup las ejecuta en el orden adecuado.

Reduce el tiempo de arranque consolidando inicializaciones de librerías y dando control sobre cuáles diferir.

Ejemplo:
class LoggerInitializer : Initializer<Logger> {
    override fun create(context: Context): Logger {
        return Logger().also { Timber.plant(it) }
    }
    override fun dependencies(): List<Class<out Initializer<*>>> = emptyList()
}

<provider
    android:name="androidx.startup.InitializationProvider"
    android:authorities="${applicationId}.androidx-startup"
    android:exported="false"
    tools:node="merge">
    <meta-data android:name="com.example.LoggerInitializer" android:value="androidx.startup"/>
</provider>

ExplicaciónKotlin:
class LoggerInitializer : Initializer<Logger> implementa interfaz genérica Initializer<Logger> de App Startup. override fun create(context: Context): Logger { return Logger().also { Timber.plant(it) } } usa also { } scope function: crea Logger(), lo pasa al lambda como it, ejecuta Timber.plant(it) y devuelve el Logger original. override fun dependencies(): List<Class<out Initializer<*>>> = emptyList() devuelve lista vacía con emptyList() inferido; Class<out Initializer<*>> usa variance out y star projection * en genéricos Java interop. El XML del provider no es Kotlin; authorities="${applicationId}" es Gradle placeholder. En Kotlin solo aparecen clases, override, generics, also y funciones que retornan tipos de Android/Java (Context, Logger).

Consejo:
"Inicializa muchas libs en un único provider con orden". Útil para mejorar el cold start y para diferir inicializaciones pesadas.
