Concepto:
Acceder a la cámara implica permiso CAMERA (y en Android 13+ lectura de medios puede implicar permisos distintos según caso), configurar CameraProvider y gestionar el ciclo de vida. La vía recomendada es CameraX (Preview, ImageCapture, ImageAnalysis) enlazada al Lifecycle.

La alternativa trivial es delegar en ACTION_IMAGE_CAPTURE si solo necesitas una foto puntual.

Ejemplo:
val provider = ProcessCameraProvider.getInstance(this).get()
val preview = Preview.Builder().build().also { it.setSurfaceProvider(previewView.surfaceProvider) }
provider.bindToLifecycle(this, CameraSelector.DEFAULT_BACK_CAMERA, preview)

Consejo:
Para permisos runtime usa ActivityResultContracts.RequestPermission. Comprueba flash, rotación y threading en ImageAnalysis (no bloquees el analyzer).
