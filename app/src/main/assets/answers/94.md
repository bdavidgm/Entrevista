## Concepto
El hilo principal (UI thread) en Android es responsable de dibujar frames, despachar eventos táctiles y ejecutar callbacks de [Activity](pregunta:31). Realizar trabajo largo o bloqueante en Main provoca jank, frames perdidos y [ANR](pregunta:95) ([Application](pregunta:128) Not Responding).

## Explicación detallada
Regla histórica: NetworkOnMainThreadException en API estricta para red en main. [Room](pregunta:70) y muchas APIs modernas exigen background. Compose [recomposición](pregunta:47) y medición ocurren en Main; cálculos pesados en composición bloquean 16ms por frame.

Solución: [coroutines](pregunta:78) [withContext](pregunta:82)(IO), Executor, [WorkManager](pregunta:72) para deferible. Actualizar UI solo en Main tras resultado. [StrictMode](pregunta:97) detecta disk/network en main en debug.

Perfis: Systrace, [Android Studio](pregunta:194) Profiler CPU. [ANR](pregunta:95) trace muestra main bloqueado.

En el contexto de proyectos Android reales, combina esta idea con las guías oficiales de Android Developers y con métricas de calidad (crashes, [ANR](pregunta:95), Vitals) cuando aplique. En code review, pregunta siempre por ciclo de vida, testabilidad y si la solución escala cuando el equipo crece.

Durante la entrevista, estructura la respuesta en definición, ejemplo concreto de tu experiencia o proyecto demo, y trade-offs frente a alternativas. Mencionar versiones recientes de API o biblioteca (Material3, [Paging 3](pregunta:76), [Hilt](pregunta:74) 2.x) demuestra que sigues el ecosistema, sin necesidad de memorizar números de versión exactos.

Para profundizar tras la entrevista, revisa documentación oficial, codelabs y el código fuente de tu propio proyecto Entrevista aplicando el concepto en un flujo mínimo reproducible. La práctica deliberada con tests unitarios refuerza la confianza al explicar el tema en viva voz.

En el contexto de proyectos Android reales, combina esta idea con las guías oficiales de Android Developers y con métricas de calidad (crashes, [ANR](pregunta:95), Vitals) cuando aplique. En code review, pregunta siempre por ciclo de vida, testabilidad y si la solución escala cuando el equipo crece.

Durante la entrevista, estructura la respuesta en definición, ejemplo concreto de tu experiencia o proyecto demo, y trade-offs frente a alternativas. Mencionar versiones recientes de API o biblioteca (Material3, [Paging 3](pregunta:76), [Hilt](pregunta:74) 2.x) demuestra que sigues el ecosistema, sin necesidad de memorizar números de versión exactos.

Para profundizar tras la entrevista, revisa documentación oficial, codelabs y el código fuente de tu propio proyecto Entrevista aplicando el concepto en un flujo mínimo reproducible. La práctica deliberada con tests unitarios refuerza la confianza al explicar el tema en viva voz.

## Cuándo usarlo
Siempre offload IO y CPU pesado. Lectura rápida [DataStore](pregunta:73) ya es async. Imagen: [Coil](pregunta:259) en background automático.

Ejemplo:
// Mal
fun onClick() {
    val json = URL(url).readText() // ANR risk
    textView.text = json
}

// Bien
fun onClick() = lifecycleScope.launch {
    val json = withContext(Dispatchers.IO) { api.fetch() }
    textView.text = json
}


ExplicaciónKotlin:
El ejemplo "Mal" muestra función `onClick()` regular (no [suspend](pregunta:79)) que llama `URL(url).readText()`: operación bloqueante de red en Main. `textView.text = json` muta la vista desde el mismo hilo. El ejemplo "Bien" usa `fun onClick() = lifecycleScope.launch { }`: expresión de cuerpo único donde `launch` devuelve `Job` y la función devuelve ese Job implícitamente. `lifecycleScope` es extensión de `LifecycleOwner` cancelada al destruir el lifecycle. `withContext(Dispatchers.IO) { api.fetch() }` cambia a pool IO para trabajo bloqueante; `api.fetch()` presumiblemente es `suspend` o bloqueante envuelto. Tras `withContext`, la [corrutina](pregunta:78) vuelve a Main (dispatcher de lanzamiento en Android), permitiendo `textView.text = json` de forma segura. `Dispatchers.IO` y `Dispatchers.Main` son singletons del objeto `Dispatchers`.

Consejo para entrevista:
Menciona 16ms frame budget y [ANR](pregunta:95). Herramientas: [StrictMode](pregunta:97), Profiler. Compose: no parses JSON en [@Composable](pregunta:46) body.
